# AddressBook
A C program to store the name, contact and email of users. Done as a project to get familiar with C programing language

Run the main.c file.
contact.c contains all the function prototypes.
contact.h contains the function definitions.
populate files are used to add default contacts to a fresh file.
csv file stores all the contacts.
